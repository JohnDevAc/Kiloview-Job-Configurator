# Job Configurator 0.8.13 / 0.8.13-dev.1 release candidate

Schema-invalid saved state now follows quarantine and valid-backup recovery. Repeated reads continue to require repair when both copies are invalid, preserving evidence instead of silently creating an empty inventory. Interrupted multicast reversion records retryable outcomes, continues monitoring unfinished endpoints, and preserves a replacement plan when an older operation finishes.

The reviewed Windows maintenance fix is included: confirmed session/Restart Manager shutdown uses normal host disposal and suppresses a pending tray restart. Cancelled shutdown queries leave the server running. No new shutdown endpoint, native-library unload or scheduled-task behavior is introduced. See WINDOWS-MAINTENANCE.md. The real isolated Restart Manager fixture passes; elevated vendor-installer acceptance and hung native calls remain qualified limitations.

Bundles the complete, separately built PC Agent 0.7.4 for Main or 0.7.4-dev.1 for Development, with clean source provenance and file hashes. PC configuration remains owned by the companion, with unchanged local schema-1 and remote approval behavior. The Agent restores configuration files after ordinary onboarding failure; abrupt process death and independent package/startup/firewall changes are outside that file transaction. Software upgrades preserve saved Agent configuration during transient APIPA and reject unusable saved inputs before installation mutations. New installations retain explicit adapter selection during later setup/onboarding.

Validated QA S1/S2 and maintenance: 26 server regressions, 13 frontend checks, installer helpers, three native lifecycle checks, companion Setup (27 labelled checks) and Agent (22), 12 interop scenarios, diagnostic HTTP and release metadata. Tests used isolated state, mock control traffic and owned processes, without installing software or changing live NDI/device configuration.

Prepared locally on 12 September 2026, replacing initial local candidates 7ec542e/67fbfcd so the bundle includes the corrected Agent upgrade. This is a candidate, not a published release or installed update. The companion must be separately published first, followed by a fresh final server build against that clean matching released commit. The server installer stops/replaces/restarts its application and refreshes its firewall rules, scheduled task and shortcuts; retain the exact prior package/application directory and state backups before an authorized upgrade. Updating software does not authorize onboarding, reapply, adapter changes or vendor NDI maintenance.
