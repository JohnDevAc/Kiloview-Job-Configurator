<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- The application uses only the .NET shared framework and restores fully offline. -->
  <packageSources>
    <clear />
  </packageSources>
</configuration>
